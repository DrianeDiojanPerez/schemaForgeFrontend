# The backend's gRPC address. The server functions dial this over native
# HTTP/2; it is never exposed to the browser.
SCHEMAFORGE_GRPC_ADDRESS="127.0.0.1:50051"

# Every SchemaService call needs a bearer token and a permission behind it.
# This server signs in once with these and keeps the token to itself.
SCHEMAFORGE_EMAIL="admin@example.com"
SCHEMAFORGE_PASSWORD="password"

# Where the backend's proto directory lives. Only needed when the backend is
# not a sibling checkout named schemaForgeBackend.
# SCHEMAFORGE_PROTO_DIR="/home/you/code/schemaForgeBackend/proto"
